# Batch: Build 5 pending Lattice specs

Each task implements one design doc under docs/superpowers/specs/2026-07-05-*.
Follow that doc's own Goal/Non-goals/Architecture/Testing/Open-decisions
sections as the authoritative spec — the summaries below are just pointers.
Run `pesde run test` (or `lune run scripts/RunTests.luau`) before finishing
and fix any failures caused by your change. Update CLAUDE.md's relevant
sections (Current Work / Completed Milestones / Implementation Notes) to
reflect what you built, per CLAUDE.md's own "Updating This File" section.

Note: PATH setup required: `export PATH="$HOME/.rokit/bin:$HOME/.local/bin:$PATH"`.

## Task 1: Extended codec coverage
Implement docs/superpowers/specs/2026-07-05-extended-codec-coverage-design.md.
Add `ProtectedString`/`ContentId` as string-codec aliases in
src/artifacts/build/resolveType.luau's LEAF_MAP, add new src/serde/ray.luau
codec (id 27, mirrors rect.luau/udim2.luau — 2x Vector3, 6 f32s), register it
in src/serde/init.luau and LEAF_MAP. Add tests/SerdeRay.spec.luau. Rebuild
the artifact and confirm the warning count drops per the spec. Document the
SecurityCapabilities limitation in CLAUDE.md if not already documented.
Skip DateTime (optional stretch per spec).
Files: src/serde/ray.luau (new), src/serde/init.luau,
src/artifacts/build/resolveType.luau, tests/SerdeRay.spec.luau,
scripts/RunTests.luau (wire in new spec), CLAUDE.md.

## Task 2: Column dictionary encoding
Implement docs/superpowers/specs/2026-07-05-column-dictionary-encoding-design.md.
Add Dictionary as 5th column encoding (id 2) in src/format/types.luau, wire
into the cost-picker in src/instance/Serializer.luau and read/write branches
in src/format/Writer.luau / src/format/Reader.luau. Add the benchmark
comparing whole-instance dedup on/off with Dictionary active; per the spec,
only remove templateRefs dedup if the benchmark shows it's not worth its
overhead — otherwise keep it and note why. Extend tests/Format.spec.luau and
tests/Instance.spec.luau per the Testing section.
Files: src/format/types.luau, src/format/Writer.luau, src/format/Reader.luau,
src/instance/Serializer.luau, tests/Format.spec.luau, tests/Instance.spec.luau,
bench/ (new fixture), CLAUDE.md.

## Task 3: Schema evolution / wire compat into read path
Implement docs/superpowers/specs/2026-07-05-schema-evolution-design.md.
Split src/format/Reader.luau's body into src/format/readers/V1.luau (frozen,
today's body) and src/format/readers/V2.luau (templateRefs-as-RLE), add
version dispatch in Reader.luau that calls compat.migrate after per-version
byte parsing. Bump Writer.luau to write version 2 with templateRefs as RLE.
Register the (no-op) v1->v2 migration in src/compat/init.luau's
defaultRegistry. Commit a frozen v1 binary fixture (generate once, check in
bytes) and add tests/SchemaEvolution.spec.luau per the spec's Testing
section. Extend tests/Compat.spec.luau with one case using the real
defaultRegistry (not a mock).
Files: src/format/Reader.luau, src/format/readers/V1.luau (new),
src/format/readers/V2.luau (new), src/format/Writer.luau, src/compat/init.luau,
tests/SchemaEvolution.spec.luau (new), tests/Compat.spec.luau,
tests/fixtures/format_v1_sample.bin (new), scripts/RunTests.luau, CLAUDE.md.

NOTE: this task touches src/format/Reader.luau and src/format/Writer.luau,
the same files Task 2 (Column Dictionary Encoding) touches. These run in
separate isolated worktrees and will NOT see each other's changes — expect a
manual merge/rebase to reconcile both changes afterward. Keep your diff to
exactly what this spec needs so the merge is tractable.

## Task 4: CLI tooling (lattice stats/dump/diff)
Implement docs/superpowers/specs/2026-07-05-cli-tooling-design.md. Build
cli/Main.luau (arg parsing/dispatch), cli/Stats.luau, cli/Dump.luau,
cli/Diff.luau, cli/format.luau (shared helpers). `stats`/`dump` read
FormatData directly (no full instance deserialize); `diff` deserializes both
files and matches instances by UniqueId with positional-fallback, printing
which strategy was used per the spec's honesty requirement. Put the
matching function in cli/Diff.luau as a pure, independently-testable function.
Wire `lattice = "cli/Main.luau"` into pesde.toml's [scripts]. Add a smoke
test running stats/dump against a known fixture, and a direct unit test for
the diff matching function.
Files: cli/Main.luau (new), cli/Stats.luau (new), cli/Dump.luau (new),
cli/Diff.luau (new), cli/format.luau (new), pesde.toml, tests/ (new smoke +
matching tests), scripts/RunTests.luau, CLAUDE.md.

## Task 5: Incremental/delta encoding (MVP: baseline + one patch)
Implement docs/superpowers/specs/2026-07-05-incremental-delta-encoding-design.md.
Build src/patch/diff.luau (compute a patch between a baseline .lattice file
and a live instance tree) and src/patch/apply.luau (apply a patch to a
baseline to reconstruct the current tree), plus the "LATP"-magic patch
format described in the spec. Extract the Enum/Referent-aware property
comparison helper out of src/instance/Serializer.luau's Pass A into a shared
location both the serializer and src/patch/diff.luau can import. Write your
own UniqueId-with-positional-fallback identity-matching logic scoped to
src/patch/ (do not assume cli/Diff.luau exists yet — this task runs in an
isolated worktree without visibility into Task 4's work; note in your PR
that this may need reconciling with cli/Diff.luau's matching function later,
per the spec's own guidance). MVP scope only — no chained patches, no
compaction, no conflict resolution, no sub-property deltas (see spec's
Non-goals). Add the round-trip tests listed in the spec's Testing section
plus the realistic-edit-session benchmark fixture.
Files: src/patch/diff.luau (new), src/patch/apply.luau (new),
src/patch/types.luau (new, if needed), tests/PatchDiff.spec.luau (new),
scripts/RunTests.luau, bench/ (new fixture), CLAUDE.md.
